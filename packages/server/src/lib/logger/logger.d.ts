export declare function logProgress(items: string[], highlightIndex?: number): void;
export declare function logNotification(msg: string): void;
export declare function logEvent(msg: string): void;

import logger from './logger';
export default logger;
